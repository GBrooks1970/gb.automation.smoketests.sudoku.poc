# How To: [Task Title]

<!-- 
  TEMPLATE INSTRUCTIONS (delete this block before publishing)
  
  A how-to guide answers ONE practical question: "How do I do X?"
  It is not a tutorial (which teaches concepts), not a reference (which lists facts),
  and not an explanation (which describes why things work).
  
  Keep it task-focused. The reader has a goal. Get them there.
  
  Naming: save as HOWTO_{Topic}.md in DOCS/.howto/
-->

**Difficulty:** <!-- Beginner | Intermediate | Advanced -->
**Time to complete:** <!-- e.g. 10 minutes -->
**Last verified:** <!-- YYYY-MM-DD -->

---

## What you will achieve

<!-- 
  One or two sentences. State the concrete outcome at the end of this guide.
  Example: "By the end of this guide you will have a VS Code debugger attached
  to the running solver, with a breakpoint hit inside hiddenSingles()."
-->

---

## Before you start

<!-- List everything the reader needs in place before step 1. Be specific. -->

**You need:**
- [ ] <!-- e.g. Node.js 16+ installed (`node --version` to check) -->
- [ ] <!-- e.g. VS Code with the TypeScript extension -->
- [ ] <!-- e.g. `npm install` run inside `demo-apps/demoapp001-typescript-cypress/` -->

**You should know:**
- <!-- e.g. How to open a terminal -->
- <!-- e.g. Basic TypeScript syntax -->

---

## Steps

### Step 1: [First action]

<!-- 
  Each step is a single, observable action. Lead with the imperative verb.
  Include the exact command, file path, or UI interaction needed.
  Follow with what the reader should see to confirm it worked.
-->

```bash
# Example command
npm install
```

**You should see:**
```
# Expected output excerpt
```

---

### Step 2: [Next action]

<!-- 
  Code blocks use the correct language identifier (bash, typescript, json, etc.)
  File paths are relative to the repo root unless stated otherwise.
-->

Open [`demo-apps/demoapp001-typescript-cypress/app_src/SudokuSolver.ts`](../../demo-apps/demoapp001-typescript-cypress/app_src/SudokuSolver.ts).

---

### Step 3: [Next action]

<!-- 
  Screenshots are optional. If the UI is complex, describe exactly where to click
  rather than relying on an image that may go stale.
-->

---

<!-- Add as many steps as needed. Keep each step atomic — one action per step. -->

---

## Verify it worked

<!-- 
  A concrete, unambiguous success check. The reader should be able to answer
  "yes" or "no" with certainty.
-->

If everything worked, you should be able to:
- [ ] <!-- Observable outcome 1 -->
- [ ] <!-- Observable outcome 2 -->

---

## Common problems

<!-- 
  List failure modes the reader is likely to hit, and their fixes.
  Use the exact error message or symptom as the heading.
-->

### `[Exact error message or symptom]`

**Cause:** <!-- What went wrong -->
**Fix:** <!-- How to recover -->

---

## What to do next

<!-- 
  One or two pointers to related guides, design docs, or the natural next task.
  Keep it brief — this section is a signpost, not a lesson.
-->

- <!-- e.g. [How to add a new puzzle](HOWTO_Add_A_New_Puzzle.md) -->
- <!-- e.g. [Algorithm documentation](../ALGORITHM_Sudoku_Basic_Solver.md) -->
