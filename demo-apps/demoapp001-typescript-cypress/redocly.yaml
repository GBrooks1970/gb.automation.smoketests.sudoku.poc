extends:
  - recommended

rules:
  # This contract deliberately documents the local demo server rather than a deployed API host.
  no-server-example.com: off
  # Health and puzzle-list operations have no implemented client-error response.
  operation-4xx-response: off
